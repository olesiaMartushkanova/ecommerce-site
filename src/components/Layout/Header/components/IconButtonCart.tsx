import { ICONS } from '../../../../utils/constants';
import Icon from '../../../Primitives/Icon/Icon';

const IconButtonCart = () => {
  return <Icon src={ICONS.cart} alt='cart icon button' width='30' />;
};
export default IconButtonCart;
