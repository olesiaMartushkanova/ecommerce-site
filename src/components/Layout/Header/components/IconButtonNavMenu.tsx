import Icon from '../../../Primitives/Icon';

const IconButtonNavMenu = () => {
  return (
    <Icon alt={'header navigation icon menu'} src='/images/menu-closed.svg' />
  );
};
export default IconButtonNavMenu;
